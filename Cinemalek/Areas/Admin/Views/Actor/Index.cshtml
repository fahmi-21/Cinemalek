
@model ActorsVM

<div class="container">


    <!-- Create butoom and title -->
    <div class="row mb-3 align-items-center my-3">
        <div class="col-md-6">
            <h2 class="display-6">Actors</h2>
        </div>

        <div class="col-md-6 text-md-end text-start mt-2 mt-md-0">
            <a href="/Admin/Actor/Create" class="btn btn-success"> + Add Actor </a>
        </div>
    </div>

    <!-- filter bar -->
    <section class="py-3 filter-bar">
        <div class="container-fluid px-0">
            <form>
                <div class="row" style="background-color: rgba(var(--bs-light-rgb) , var(--bs-bg-opacity)) !
                important;">
                    <div class="col-md-5 mb-2 mb-md-0">
                        <input type="text" class="form-control" placeholder="Actor Name ....." name="actorname">
                    </div>
                    <div class="col-md-4 text-md-start">
                        <button class="btn btn-primary">Filter</button>
                    </div>
                </div>
            </form>
        </div>
    </section>

    <!-- table  -->
    <div class="row">
        <div class="col-12">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Movies</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model.Actors)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.Name</td>
                            <td>
                                <a href="/Admin/Actor/Details/@item.Id"class="btn btn-info btn-sm"title="View Details"><i class="fa-solid fa-eye"></i> </a>
                                <a href="/Admin/Actor/Edit/@item.Id" class="btn btn-sm btn-warning btn-edit"><i class="fa-solid fa-pen"></i></a>
                                <a href="/Admin/Actor/Delete/@item.Id" class="btn btn-sm btn-danger btn-delete"><i class="fa-solid fa-trash"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12 d-flex justify-content-center">
            <!-- Pagination Placeholder -->
            <nav aria-label="Page navigation example">
                <ul class="pagination justify-content-center">

                    @if (Model.CurrentPage > 1)
                    {
                        <li class="page-item"><a class="page-link" href="/Admin/Actor/Index?page=@(Model.CurrentPage - 1)" tabindex="-1">Previous</a></li>
                    }

                    @for (int i = 1; i <= Model.TotalPages; i++)
                    {
                        @if (i == Model.CurrentPage)
                        {
                            <li class="page-item"><a class="page-link active" href="/Admin/Actor/Index?page=@i"> @i </a></li>
                        }
                        else
                        {
                            <li class="page-item"><a class="page-link" href="/Admin/Actor/Index?page=@i"> @i </a></li>
                        }
                    }

                    @if (Model.CurrentPage < Model.TotalPages)
                    {
                        <li class="page-item"><a class="page-link" href="/Admin/Actor/Index?page=@(Model.CurrentPage + 1)">Next</a></li>
                    }

                </ul>
        </div>
    </div>
</div>



<script>
    document.querySelectorAll(".btn-delete").forEach(btn => {
        btn.addEventListener("click", function(e) {
            e.preventDefault();
            const url = this.getAttribute("href");

            Swal.fire({
                title: "Are you sure?",
                text: "You won't be able to revert this!",
                icon: "warning",
                showCancelButton: true,
                confirmButtonText: "Yes, delete it!"
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = url;
                }
            });
        });
    });
</script>
